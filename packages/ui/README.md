# @inventto/ui

Design system compartilhado entre os apps (`stores`, `storefront`, e futuros `admin`/`crm`).

Ainda vazio — placeholder criado junto com o scaffold do monorepo. Os componentes shadcn/ui
hoje vivem em `apps/stores/src/shared/ui` (ou equivalente); a extração pra cá acontece quando um
segundo app precisar consumi-los de fato.
