# @inventto/config

Configs compartilhadas (tsconfig base, eslint base, tailwind preset) entre os apps.

Ainda vazio — placeholder criado junto com o scaffold do monorepo. Cada app mantém sua própria
config por enquanto; a extração pra cá acontece quando houver duplicação real entre `stores` e
`storefront`.
