# @inventto/supabase-types

Tipos gerados do schema do Supabase (`supabase gen types typescript`), compartilhados entre os
apps que falam com o mesmo backend.

Ainda vazio — placeholder criado junto com o scaffold do monorepo. Os tipos hoje são gerados
dentro de `apps/stores`; a extração pra cá acontece quando o `storefront` precisar dos mesmos
tipos.
